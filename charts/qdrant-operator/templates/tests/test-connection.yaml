apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qdrant-operator.fullname" . }}-test-connection"
  labels:
    {{- include "qdrant-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qdrant-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
